# Shell scripts must stay LF: some (e.g. the Arius.Cli Update helpers) are read
# from disk, re-emitted, and executed via /bin/sh, where a stray CR would break
# the shebang and the wait/rename flow.
*.sh text eol=lf
